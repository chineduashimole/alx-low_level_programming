#include "main.h"

/**
 * _isupper - this function checks if a letter is upper
 * @x: the number to be checked
 * return: 1 if upper, else 0 
 */

int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
